{
  "name": "antools-live-tracker",
  "version": "2.0.0",
  "private": true,
  "description": "ShareLoc live vehicle tracker: telemetry ingest, HERE enrichment and spectator map",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "simulate": "node scripts/simulate-drive.js"
  }
}
